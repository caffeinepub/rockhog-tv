{
  "kind": "build_request",
  "title": "Delete Bacon Ops Arena",
  "projectName": "RockHog TV",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Do not edit any frontend files under the immutable paths listed in SYSTEM_CONTEXT (compose existing UI components instead).",
    "Backend must remain a single Motoko actor (backend/main.mo); only add backend migration code (backend/migration.mo) if required for an upgrade.",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Do not add a replacement game or new game features.",
    "Do not introduce new backend services, external databases, or third-party authentication providers beyond Internet Identity."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}